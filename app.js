(async()=>{const files=Array.from({length:10},(_,i)=>`chunks/app.${String(i).padStart(2,'0')}.txt`);const b64=(await Promise.all(files.map(file=>fetch(file).then(res=>{if(!res.ok)throw new Error('Failed to load '+file);return res.text();})))).join('');const bytes=Uint8Array.from(atob(b64),c=>c.charCodeAt(0));const stream=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'));const source=await new Response(stream).text();(0,eval)(source);})().catch(error=>{document.body.innerHTML='<main style="font-family:system-ui,sans-serif;padding:24px"><h1>Load failed</h1><p>'+String(error.message||error)+'</p></main>';console.error(error);});
